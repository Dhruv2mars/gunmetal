# Keep one provider connection per upstream provider

Gunmetal intentionally supports one provider connection per upstream provider in the product model. This keeps setup, Gunmetal key usage, model routing, and dashboard state simple; multi-account or multi-profile provider workflows are out of scope unless a strong user need appears.
