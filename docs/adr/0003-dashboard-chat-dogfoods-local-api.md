# Make Dashboard chat dogfood the Local API

The Chat playground must use a Gunmetal key, provider-qualified model ID, and Local API path like an external app. This adds some implementation friction, but it lets users validate the exact API key, provider connection, model routing, and request path their apps will use.
