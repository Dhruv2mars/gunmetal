# Keep Gunmetal single-user and local-first

Gunmetal is a single-user local-first product: provider credentials, Gunmetal keys, synced models, request logs, and dashboard state live on the user's machine. We are not adding a Gunmetal account, team workspace, hosted dashboard, or cloud control plane because the core product promise is to route a user's existing provider access through one local API.
